# Priori

A read-only dashboard for the `SignalLedger` contract. Reads directly from chain over a public RPC endpoint via `ethers` -- no backend, no API keys, no wallet connection.

See the [Frontend section of the repo README](../README.md#frontend) for local dev setup and Vercel deployment instructions.
